программа ДвоичныйФайл;
переменные
    ф: объект;
    с: символ;
    ст: строка;
    ц: целое;
    д: дробное;
    м: момент;
    л: логическое;
начало
    ф := файлСоздать(фтДвоичный, "/home/czaerlag/тест.дв", фcдЧтение);
    начало
        файлЗаписать(ф, 'A', "ABC", -1, максДробное, сейчас(), да, ф);
        файлПоз(ф, 0);
        файлПрочесть(ф, с, ст, ц, д, м, л);
    напоследок
        файлЗакрыть(ф);
    конец;
окончание.
