программа Эхо(арг: массив строк);
переменная
    к: целое;
    рзд: строка = "";
начало
    для к от 0 до длина(арг)-1 цикл начало
        вывести(рзд, арг[к]);
        рзд := " ";
    окончание;
    вывести(НС);
окончание.
