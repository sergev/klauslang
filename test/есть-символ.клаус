программа ЕстьСимв;
переменная
    с: символ;
начало
    пока естьСимвол() цикл начало
        с := прочестьСимвол();
        если с = КФ то начало
            вывести("<КФ>");
            завершить 0;
        конец иначе 
            вывести(с);
    конец;
окончание.
