программа ТестМышки;

используются Файлы, Мышка;

процедура пройти();
начало
    пока не закрашено() цикл начало
        закрасить();
        если стрелка() <> 0 то шаг(стрелка())
        иначе если не стена(справа) то шаг(вправо)
        иначе если не стена(впереди) то шаг(вперёд)
        иначе если не стена(слева) то шаг(влево)
        иначе шаг(назад);
    конец;
окончание;

переменная
    п: строка;
начало
    п := файлПуть(файлВыполняемый());
    загрузитьОбстановку(п  ++ "test.klaus-setting", 0);
    пройти();
    загрузитьОбстановку(п  ++ "test.klaus-setting", 1);
    пройти();
окончание.
