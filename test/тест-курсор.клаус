программа ТестКурсор;
переменная
    к: целое;
начало
    для к от 1 до 30 цикл начало
        подвинутьКурсор(0, 1);
        пауза(20);
    конец;
    для к от 1 до 40 цикл начало
        подвинутьКурсор(0, -1);
        пауза(20);
    конец;
    /*для к от 1 до 200 цикл начало
        подвинутьКурсор(1, 0);
        пауза(20);
    конец;
    для к от 1 до 200 цикл начало
        подвинутьКурсор(-2, 0);
        пауза(20);
    конец;*/
окончание.
