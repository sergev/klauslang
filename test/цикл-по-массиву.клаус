программа ЦиклПоМассиву;
переменные
    м: массив строк;
    кво, и: целое;
начало
    сообщить("Введите количество: ");
    ввести(кво);
    длина(м, кво);
    для каждого и из м цикл начало
        сообщить("Введите элемент ", и, ": ");
        ввести(м[и]);
    конец;
    сообщить("Значения в обратном порядке:", НС);
    для каждого и из м обратный цикл вывести(м[и], НС);
окончание.
