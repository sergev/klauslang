программа Эхо(арг: массив строк);
переменная
    и: целое;
    рзд: строка = "";
начало
    для и от 0 до длина(арг)-1 цикл начало
        вывести(рзд, арг[и]);
        рзд := " ";
    окончание;
    вывести(НС);
окончание.
