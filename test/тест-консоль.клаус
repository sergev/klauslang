программа ТестКонсоль;
переменная
    с: строка;
    и: целое;
начало
    с := "";
    для и от 0 до 99 цикл с := с ++ "АБ";
    вывести(с, НС, "Введите строку: ");
    ввести(с);
    вывести(с, НС);
окончание.
