программа примерЦикла;
переменная
    к, кво: целое;
начало
    вывести("Введите кол-во итераций: ");
    ввести(кво);
    для к от 0 до кво-1 цикл 
        вывести("Итерация ", к, НС);
    вывести("А теперь обратно:", НС);
    для к от кво-1 до 0 обратный цикл 
        вывести("Итерация ", к, НС);
окончание.
