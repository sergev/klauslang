программа ЕстьНету;
переменная
    сл: словарь строк ключ строка;
    м: массив дробных;
    к: целое;
начало
    если есть(сл["первое"]) то сл["первое"] := "есть";
    если нету(сл["первое"]) то сл["первое"] := "нету";
    если есть(сл["первое"]) то сл["первое"] := "было";
    если нету(сл["первое"]) то сл["первое"] := "не было";
    длина(м, 3);
    для к от 0 до 10 цикл
        если есть(м[к]) то м[к] := к + 0.5
        иначе прервать;       
    ничего;
окончание.
