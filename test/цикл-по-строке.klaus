программа ЦиклПоСтроке;
переменная
    б: целое;
    с: строка;
    смв: символ;
начало
    вывести("Введите строку: ");
    ввести(с);
    для каждого б из с цикл начало
        смв := симв(с, б);
        вывести(б, ": ", смв, НС);
    конец;
окончание.
