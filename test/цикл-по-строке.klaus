программа ЦиклПоСтроке;
переменные
    Б: целое;
    С: строка;
    смв: символ;
начало
    сообщить("Введите строку: ");
    ввести(С);
    для каждого Б из С цикл начало
        смв := С[Б];
        вывести(Б, ": ", смв, НС);
    конец;
окончание.
