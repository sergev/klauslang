программа ЦиклПока;
переменная
    к: целое;
начало
    к := 0;
    пока к < 10 цикл начало
        вывести(к, " ");
        к += 1;
    окончание;
    вывести(НС);
    цикл начало
        к -= 1;
        вывести(к, " ");
    окончание пока к > 0;
    вывести(НС);
окончание.
