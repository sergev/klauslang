программа Пример;
переменные
    смв: символ = №4350;
    стр: строка = "Строковый литерал";
    цел: целое = 12345;
    дрб: дробное = 123.45э67;
    ммт: момент = `2023-05-17 14:03`;
начало
    // однострочный комментарий
    дрб := дрб / (цел + целое(смв));
    если дрб > 0 то начало
        вывести("дрб = " ++ строка(дрб), НС);
        вывести(стр, НС);
    конец;
   /*многострочный комментарий
    а на следующей строке недопустимые символы*/
    
    // строка с ошибкой
    
    // точка останова
    
    // выполняемая строка в отладчике
окончание.
