программа ЦиклПоСтроке;
переменные
    к, кво: целое;
    с: строка = "АБВГД"#0"01234Ф";
начало
    кво := 0;
    для каждого к из с цикл начало
        вывести(с[к]);
        кво += 1;
    конец;
    вывести(НС);
    для к от 0 до длина(с)-1 цикл
        если есть(с[к]) то вывести(с[к]);
    вывести(НС);
    повторить кво раз вывести("-");
окончание.
