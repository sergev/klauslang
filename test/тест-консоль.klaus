программа ТестКонсоль;
переменная
    с: строка;
    к: целое;
начало
    с := "";
    для к от 0 до 99 цикл с := с ++ "АБ";
    вывести(с, НС, "Введите строку: ");
    ввести(с);
    вывести(с, НС);
окончание.
