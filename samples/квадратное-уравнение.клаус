программа КвадратноеУравнение;
исключение
    МалоДанных(надо: целое) сообщение "Мало данных. Требуется ввести чисел: %(надо)";
переменные
    кво: целое;
    a, b, c: дробное;
    d: дробное;
    x1, x2: дробное;
начало
    сообщить("Для уравнения вида ax^2 + bx + c = 0 введите коэффициенты a, b и c.", №0а);
    сообщить("Дробные числа через пробел: ");
    кво := ввести(a, b, c);
    если кво < 3 то ошибка МалоДанных(3);
    d := b^2 - 4*a*c;
    если d < 0 то
        вывести("Уравнение не имеет действительных корней.")
    иначе начало 
        x1 := (-b + d^(1/2))/(2*a); 
        x2 := (-b - d^(1/2))/(2*a);
        вывести("x1 = ", x1, НС);
        вывести("x2 = ", x2, НС);
    конец;
    завершить 0;
исключения
    когда о: МалоДанных тогда начало
        о.текст := о.текст ++ ", а Вы ввели " ++ строка(кво) ++ ". Как не стыдно?!";
        бросить;
    конец;
окончание.
